{
    // General settings
    "layer": "top",
    "position": "top",
    "margin-bottom": 0,
    "margin-top": 4,
    "spacing": 8,

    // Left modules
    "modules-left": [
        "custom/arch",
        "group/hardware",
        "hyprland/window"
    ],

    // Middle modules
    "modules-center": [
        "hyprland/workspaces"
    ],

    // Right modules
    "modules-right": [
        "wlr/taskbar",
        "idle_inhibitor",
        "network",
        "wireplumber",
        "clock",
        "custom/power"
    ],

    // Hyprland utils
    "hyprland/workspaces": {
        "sort-by-name": false,
        "sort-by-coordinates": true,
        "on-click": "activate",
        "persistent-workspaces": {
            "HDMI-A-2": 5,
            "DP-3": [5]
        }
    },

    "hyprland/window": {
        "format": "{}",
        "icon": true,
        "icon-size": 20,
        "max-length": 32,
        "separate-outputs": true
    },

    // Taskbar
    "wlr/taskbar": {
        "format": "{icon}",
        "icon-size": 16,
        "tooltip-format": "{title}",
        "on-click": "activate",
        "on-click-middle": "close",
        "ignore-list": [
            "kitty"
        ]
    },

    // Clock
    "clock": {
        "locale": "C", 
        "format": "   {:%I:%M %p}",
        "format-alt": "   {:%A, %d %b}" // Icon: calendar-alt
    },

    // Group/Hardware
    "group/hardware": {
        "orientation": "horizontal",
        "modules": [
            "cpu",
            "memory"
        ]
    },

    "cpu": {
        "format": "&#8239;  {usage}%",
        "tooltip": false,
        "on-click": "kitty -e 'btop'"
    },
    
    "memory": {
        "interval": 30,
        "format": "  {used:0.2f}GB",
        "max-length": 10,
        "tooltip": false,
        "warning": 70,
        "critical": 90
    },

    // Network
    "network": {
        "interval": 2,
        "format-wifi": "    {essid}",
        "format-ethernet": "",
        "format-linked": "{ipaddr}/{cidr}",
        "format-disconnected": " Disconnected",
        "format-disabled": "",
        "tooltip": false,
        "min-length": 6,
        "format-alt": "  {bandwidthUpBytes} /   {bandwidthDownBytes} ({ipaddr}/{cidr})"
    },
    
    // Idle
    "idle_inhibitor": {
        "format": "{icon}",
        "format-icons": {
            "activated": "",
            "deactivated": ""
        }
    },

    // Audio
    "wireplumber": {
        // "scroll-step": 1, // %, can be a float
        "format": "{icon}    {volume}%  {format_source}",
        "format-bluetooth": "{icon}    {volume}%  {format_source}",
        "format-bluetooth-muted": "   {format_source}",
        "format-muted": "   {format_source}",
        "format-source": "",
        "format-source-muted": "",
        "format-icons": {
            "headphone": "",
            "hands-free": "",
            "headset": "",
            "phone": "",
            "portable": "",
            "car": "",
            "default": ["", "", ""]
        },
        "on-click": "pwvucontrol"
    },

    // Custom modules
    // Arch
    "custom/arch": {
        "format": "󰣇",
        "tooltip": true,
        "tooltip-format": "btw"
    },

    "custom/power": {
        "format": "⏻",
        "tooltip": false
    }
}
